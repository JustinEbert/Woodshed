// AI trainer text line — top bar, right of beat pulse
// Stub: will be implemented in AI Trainer epic
// Tap → voice input (Web Speech API)
// Offline: shows "offline — self-directed mode"

export default function TrainerLine() {
  return <span>offline — self-directed mode</span>
}
