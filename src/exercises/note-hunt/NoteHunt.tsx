// Note Hunt exercise — find all instances of a note on the fretboard
// Deferred: later epic after Note Flash is complete

export default function NoteHunt() {
  return <div>NoteHunt — coming soon</div>
}
